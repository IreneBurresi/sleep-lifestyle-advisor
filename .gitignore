# python
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.ruff_cache/
.ty_cache/

# data and artifacts: the CC0 dataset and the fitted model are committed so that the
# repository runs without a download and without a refit; the source PDFs are not (17 MB,
# and rag/index holds what is built from them)
data/*
!data/raw/
data/raw/*
!data/raw/sleep_health.csv
artifacts/*
!artifacts/model.joblib
!artifacts/profiles.json
!artifacts/metadata.json

# secrets
.env

# macos
.DS_Store

# editor
.vscode/

# agent skills, local only
.agents/
