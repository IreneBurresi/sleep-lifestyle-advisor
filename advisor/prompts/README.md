One directory per prompt version: `system.md` (instructions) and `user.md.j2` (Jinja template
rendered from an `Assignment`). `v2` is the version in use. `v1` is kept as the "before" of
the documented iteration (`docs/GUIDANCE_NOTES.md`) and is not meant to be used.
